// Medusa v2.12.3 uses CLI commands (medusa start) which reads from medusa-config.ts
// Custom middleware and routes should be added via plugins or medusa-config.ts
// This file is kept for compatibility but medusa start will use medusa-config.ts

console.log("Starting Medusa server...");
console.log("Use 'medusa start' command to start the server");
console.log("Custom routes should be added via plugins in medusa-config.ts");
